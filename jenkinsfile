pipeline {
    agent any 
    stages {
        stage('clone') { 
            steps {
                sh "rm -rf *"
                sh "git clone https://github.com/anasbr22/test-jenkins-python"W
            }
        }
        stage('Run Tests') {
            steps {
                // Exécuter les tests unitaires
                sh 'python -m unittest  test_app.py'
            }
        }
       
    }
}
